org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental compilation is disabled because its cache files
# (caches-jvm/jvm/kotlin/*.tab) cannot be closed reliably on this Windows setup —
# plugin subprojects fail with "Could not close incremental caches". Plugin Kotlin
# sources never change between our builds, so incremental compilation buys us
# nothing here anyway.
kotlin.incremental=false
